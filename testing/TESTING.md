# Testing Reasoning

The happy-path test was chosen to verify that valid vehicle data is successfully stored and returned by the system. The failure test ensures that invalid input, such as an unrealistic year value, is correctly rejected.

These tests help prevent defects related to invalid or corrupt data entering the system and ensure that business rules are consistently enforced.
